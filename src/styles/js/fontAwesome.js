import { library, icon } from '@fortawesome/fontawesome-svg-core';
import { faPlus } from '@fortawesome/free-solid-svg-icons';

library.add(faPlus);

const plus = icon({ prefix: 'fas', iconName: 'plus' });

const Icons = () => {
	return <div></div>;
};

export default Icons;
